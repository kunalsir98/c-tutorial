# include <stdio.h>
# include <stdlib.h>

struct node {
    int data;
    struct node *head;
}

int main()
{
    struct node *head=NULL , *temp

    if (head=NULL);
        printf("List is Empty")

    else{
        temp=head;
        head=head->next;
        free(temp);
        print("Delete from Begning");
    }
    return 0;
}