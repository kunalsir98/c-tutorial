#include <stdio.h>
#define SIZE 5

int queue [SIZE]
int front=-1 , rear=-1

void enqueue(int value){
    if(front=SIZE-1){
        printf("Queoverflow")
    }else{
        if(front==-1);
            front=0
        rear++
        queue[rear]==value
        printf(value)
        

    }
}
void dequeue(int value){
    int(front=-1||front>rear){
        printf("queunderflow");
    }else{
        printf(queue[front])
        front++
    }
}

int main(){
    enqueue(10);
    enqueue(20);
    deque()
}

