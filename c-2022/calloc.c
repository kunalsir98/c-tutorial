# include <stdio.h>
# include <stdlib.h>

int main(){
    int *ptr;

    ptr=(int *)calloc(5,sizeof(int));

    if(ptr=NULL){
        printf("memort allocation failed");
        return1 

    for(i=0,i<5,i++){
        printf("%d",ptr[i]);
    }

    ptr(free)
    }

    return 0;


}